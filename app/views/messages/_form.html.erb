<%= form_for @message do |f| %>

  <%= f.label :recipients %>
  <%= f.collection_select(:recipients, User.all, :id, :email, { },
                          {:multiple => true}) %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.submit %>

<% end %>


