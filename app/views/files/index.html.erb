<div class="container">
  <div class="inner-container">
  
    <h1> <%= image_tag "Logo_of_Google_Drive_small.png", :class => "google-drive-logo" %>  Files: </h1>

    <% if @files %>
      <table id="file-list" class="span12">
        <thead>
          <td> Name </td>
          <td style="width: 135px;"> Updated at </td>
        </thead>

        <tbody>
          <% @files.each do |file| %>
            <tr>
              <td> <%= link_to file.name, file_path(file) %> </td>
              <td> <%= file.updated_at.strftime("%H:%M %d %b %Y") %> </td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <%= clear nil, :height => 70 %>
    <% end %>

    <%= form_tag upload_files_path, { :multipart => true } do %>
      <%= file_field_tag 'upload[file]' %>
      <br>
      <%= submit_tag "Upload" %>
    <% end %>
  </div>

</div>