<% @result.data.items.each do |item| %>
<%= link_to item.title, item.alternateLink %>
<% debug(item) %>
<% end %>
<%= form_tag '/files_sharing/upload', { :multipart => true } do %>
    <p>
    <%= file_field_tag 'upload[file]' %>
    </p>
    <p>
    	<%= submit_tag "Upload" %>
    </p>
<% end %>