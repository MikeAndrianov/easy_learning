<div class="container" id="main">
<h1>subjects list</h1>
<ul>
      <% @subjects.each do |subject| %>
        <li>
          <h3><%= link_to subject.name, subject_surveys_path(subject) %></h3>
        </li>
      <% end %>
    </ul>
  </div>